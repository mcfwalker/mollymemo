# Session: 2026-04-25_0909

**Agent:** Claude Opus 4.7 (1M context)

## Focus
Cross-project research session: query MollyMemo's captured items for signal applicable to **sidespace-v2 Feature #19 (MCP Tool Consolidation)**. Deliverable was an enrichment of the F#19 design doc, not a code change in this repo.

## What Was Done
- Queried `items` table directly via Supabase MCP (mollymemo project `uygkxicupbvnfdcymyge`) — `search_items` RPC still doesn't exist (MOL-46 still high priority), so all retrieval was raw SQL.
- Pulled 67 candidate items via title/summary/tag ILIKE filter on MCP / agent / CLI / tool-use / context-engineering keywords across 407 total items.
- Read full transcripts of the top 12 hits (#44, #90, #109, #144, #238, #261, #262, #263, #280, #283, #311, #340, #416, #425) to extract specific actionable claims.
- Mapped findings against `sidespace-v2/docs/plans/2026-04-15-mcp-tool-consolidation-design.md` and the F#19 ticket chain (SSV-161 through SSV-168).
- Appended a "Molly Findings" section to the F#19 design doc (committed in sidespace-v2 as `793e15e`) covering:
  - Resolves Open Q #1 — `z.discriminatedUnion("kind", [...])` pattern (item #262, GitHub Engineering)
  - External validation of consolidation thesis (#416 "MCP tax" framing, #283 arxiv evidence)
  - **New Tier 2 axis** — response-body bloat (#311, #340) is orthogonal to tool-definition bloat; SSV-168 should measure both
  - Sharpens SSV-167 — minimalism trio (#283, #261, #280) argues against schema enumeration in CLAUDE.md
  - Validates archive-vs-delete `hard: boolean` proposal
  - Flags progressive disclosure (Anthropic Skills, #90) as the next lever after consolidation lands

## Commits
| Repo | Hash | Message |
|------|------|---------|
| sidespace-v2 | 793e15e | docs: cross-reference MollyMemo findings into F#19 design |

No commits in mollymemo this session (other than this log).

## Tech Debt Identified
- [ ] **MOL-46 / MOL-47** still blocking — direct SQL queries against `items` work fine, but every cross-project research session like this pays the cost of skipping the proper search path. Reconfirms high priority.
- [ ] No way to filter `items` by extracted topic vector similarity yet — all surfacing was keyword ILIKE, which missed semantic matches. Embeddings exist on the column but no `match_items` invocation from Sidespace MCP.

## CLAUDE.md Updates Needed
None.

---

## Next Session

### Suggested Focus
Resume the previously-suggested focus from `2026-02-27_1440.md`: **MOL-46 / MOL-47 (search RPCs)** — still high priority and still in `todo`. This session's experience reinforces the value: every time an agent in another project wants MollyMemo signal, they currently fall back to raw SQL because `search_mollymemo` is broken. Implementing these RPCs unblocks the whole agent knowledge tool chain (MOL-48–54).

Alternatively, if that's blocked or stale: MOL-44/45 (article web clipping, LinkedIn ingestion) for content pipeline coverage.

### Key Files
- `src/lib/openai-client.ts` — Shared OpenAI client (use for any embedding calls in `search_items` impl)
- Supabase migrations directory — `match_items` RPC already exists; new RPCs should compose with it via vector search + ILIKE keyword fallback
- `sidespace-v2/docs/plans/2026-04-15-mcp-tool-consolidation-design.md` — Reference for how MollyMemo data enriches Sidespace planning (this session's deliverable)

### Build Plan Status
Feature #7 (Tech Debt Spike) complete as of 2026-02-27. No new MollyMemo work shipped this session. Top of `todo` queue:

| Priority | Identifier | Title |
|---|---|---|
| high | MOL-46 | Implement `search_items` RPC function |
| high | MOL-47 | Implement `search_items_in_container` RPC function |
| medium | MOL-48–54 | Agent knowledge tool chain (depends on MOL-46/47) |
| medium | MOL-44/45 | Content pipeline (article clipping, LinkedIn) |

(Verified live against Sidespace tracker at session start — see workspace state snapshot 2026-04-25 08:54.)

### Kickoff Message
Last session was a cross-project research detour: queried MollyMemo's `items` table for MCP/CLI/agent-tooling signal and applied 8 entries to the sidespace-v2 Feature #19 design doc (committed there as `793e15e`, no mollymemo changes). The detour reinforced that **MOL-46/47 are still the highest-priority unblocker** — every external agent that wants MollyMemo signal currently has to fall back to raw SQL because `search_mollymemo` MCP tool calls a non-existent `search_items` RPC. `match_items` embedding RPC already exists; implementing `search_items` is mostly composing the existing embedding query with keyword ILIKE fallback. Pick this up, or pivot to MOL-44/45 if blocked.